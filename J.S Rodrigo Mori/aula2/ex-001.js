

/*    if (SE)
      else (SENÃO)

          OPERADORES

          > maior que
          < menor que
          == igual
          === igual com comparação (tipo)
          >= maior ou igual que
          <= menor ou igual que
 */

          const aluno = 4


          if (aluno >= 8) {
            console.log ("Aluno Acima da média - Aprovado")
          }

          else if  (aluno >= 5) {
            console.log ("Aluno Está abaixo da média - Aprovado Com Algumas Restrições")
          }

          else if (aluno >= 3) {
            console.log ("Aluno Precisa estudar bastante - Reprovado")
          }

          else  {
            console.log ("Aluno Muito ruim - Precisa fazer o curso novamente - Reprovado")}
          
